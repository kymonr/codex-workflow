interface:
  display_name: "Native Dispatch"
  short_description: "Adaptive native dispatch and deep review"
  default_prompt: "Use $dispatching-native-agents to run adaptive native-agent dispatch or a deep review with evidence-bound claims."
policy:
  allow_implicit_invocation: true
